# poster template — not written yet

Conference poster template.

Placeholder so the slot exists. To fill it: add `main.tex` here, reuse the
shared logos from the `gridlab` package by bare file name, and load the
package with the same `\IfFileExists` pattern as `templates/beamer/main.tex`
so the template stays portable to Overleaf.
